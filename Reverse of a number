#include<stdio.h>
#include<string.h>
#include<math.h>
#include<stdlib.h>

int main()
{
   int n,sum=0,r;
   printf("enter the n value:");
   scanf("%d",&n);
   while(n>0)
   {
       r=n%10;
       sum=sum*10+r;
       n=n/10;
   }
   printf("%d",sum);
}
