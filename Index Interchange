#include<stdio.h>

int main()
{
    int i,n,b;
    printf("enter n value:");
    scanf("%d",&n);
    int arr[n],k[n];
    for(i=0;i<n;i++)
    {
       scanf("%d",&arr[i]);
       b=arr[i];
       k[b]=i;
    }
    for(i=0;i<n;i++)
       printf("%d",k[i]);

}
